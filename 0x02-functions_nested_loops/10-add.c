#include "main.h"
/**
 * add - Adds two intergers
 * Return: 0 On successful exexcution
 * @m:parameter to add
 * @v:parameter to add with
 */
int add(int m, int v)
{
	int sum;

	sum = m + v;
	return (sum);
}
