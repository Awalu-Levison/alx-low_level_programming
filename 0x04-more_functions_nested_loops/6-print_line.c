#include "main.h"
/**
 * print_line - A function that prints a line to the terminal
 * @n:Parameter to check
 */
void print_line(int n)
{
	if (n > 0)
	{
		_putchar('_');
	}
	else if (n < 0 && n == 0)
	{
		_putchar('\n');
	}
	else
	{
		_putchar('\n');
	}
}
