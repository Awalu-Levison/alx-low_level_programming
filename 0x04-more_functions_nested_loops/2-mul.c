#include "main.h"
/**
 * mul - Multipies two intergers
 * @a:First parameter
 * @b:Second parameter
 * Return: (0 on success)
 */
int mul(int a, int b)
{
	int product;

	product = a * b);

	return (product);
	return (0);
}
