#include <stdio.h>
/**
 * main - Entry point of program execution
 * Description - Generating text lines with comma as special character
 * Return:0 Always success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
