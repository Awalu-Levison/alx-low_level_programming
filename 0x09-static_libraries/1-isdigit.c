#include "main.h"
/**
 ** _isdigit - Checking if the passed argument is digit or not 0
 ** @c:Parameter to be used
 ** Return: 0 On success
 **/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
